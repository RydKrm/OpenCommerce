model Category {
  id           Int         @id @default(autoincrement())
  name         String
  description  String
  parentId     Int?
  totalItem    Int         @map("total_item")

  Product      Product[]
}